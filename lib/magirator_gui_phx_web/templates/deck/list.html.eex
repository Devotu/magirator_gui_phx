<h4>Decks</h4>

<ul>
  <%= for deck <- @decks do %>
    <li> 
      <%= deck.name %>
      <%= link "Show", to: deck_path(@conn, :show, deck.id) %>
    </li>
  <% end %>
</ul>

<%= button("Main", to: main_path(@conn, :main), method: "get") %>

